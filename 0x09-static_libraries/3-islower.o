#include "main.h"
/**
*_islower - checks if the character is in lowercase
*@c : character to be verified
*Retuer: returen 0 or 1
*/
int _islower(int c)
{
	if ((c >= 'a') && (c <= 'z')
		return (1);
	return (0);
}
